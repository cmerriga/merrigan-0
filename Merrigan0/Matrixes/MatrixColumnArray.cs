using System;
using System.Collections.Generic;
using System.Diagnostics;

namespace Merrigan0.Internal.Matrixes {
    //// When you have a Matrix and need an Array representing one column.
    //[Untested]
    //internal class MatrixColumnArray<T> : Array<T> {
    //    private Matrix<T> matrix;
    //    private long c;

    //    public override long Length { get { return matrix.Height; } }

    //    public MatrixColumnArray(Matrix<T> matrix, long c) {
    //        this.c = c;
    //        this.matrix = matrix;
    //    }

    //    public override bool TryGetItem(long i, out T item) {
    //        return matrix.TryGetItem(i, c, out item);
    //    }
    //}
}
