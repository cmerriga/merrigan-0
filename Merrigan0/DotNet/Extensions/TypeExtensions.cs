using System;
using System.Collections.Generic;
using System.Diagnostics;

namespace Merrigan0.Internal.DotNet.Extensions {
    [Untested]
    public static class TypeExtensions {
        ////public static bool HasInterface<T>(this Type type) {
        ////    return Reflection.HasInterface<T>(type);
        ////}

        ////public static bool HasInterface(this Type type, Type @interface) {
        ////    return Reflection.HasInterface(type, @interface);
        ////}

        ////public static bool IsGeneric(this Type type) {
        ////    return Reflection.Generic(type);
        ////}
    }
}
