using System;
using System.Collections.Generic;
using System.Diagnostics;

namespace Merrigan0.Internal.Matrixes {
    // When you have a Matrix and need an Array representing one column.
    //[Untested]
    //internal class MatrixRowArray<T> : Array<T> {
    //    private Matrix<T> matrix;
    //    private long r;

    //    public override long Length { get { return matrix.Width; } }

    //    public MatrixRowArray(Matrix<T> matrix, long r) {
    //        this.matrix = matrix;
    //        this.r = r;
    //    }

    //    public override bool TryGetItem(long i, out T item) {
    //        return matrix.TryGetItem(r, i, out item);
    //    }
    //}
}
