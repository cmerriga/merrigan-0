using System;
using System.Collections.Generic;
using System.Diagnostics;

////namespace Merrigan0.Internal.Nodes {
////    // Use when you have a Map but need a Node.
////    [Untested]
////    internal class MapNode : Node {
////        private Map<String, Node> map;

////        public override long Length { get { return map.Length; } }

////        public MapNode(Map<String, Node> map) { this.map = map; }

////        public bool TryGetValue(String from, out Node to) { return map.TryGetValue(from, out to); }

////        public override IDictionary<String, Node> ToDictionary() { return map.ToDictionary(); }
////    }
////}
