using System;
using System.Collections.Generic;
using System.Diagnostics;

namespace Merrigan0 {
    /////// <summary>
    /////// Equivalent to a JavaScript object. Could be an object, a loadedItems, or an loadedItems.
    /////// </summary>
    ////[Untested]
    ////public class MutableNode : Node {
    ////    public Node Current { get; protected set; }
    ////    public override long Length { get { return Domain.Length; } }

    ////    public virtual void Set(String name, Node value) { Current = new EnsureInNode(name, value); }
    ////    public virtual void Set(params object[] namesAndValues) { Current = new EnsureInNode(Current, namesAndValues); }
    ////    public override bool TryGetValue(String from, out Node to) { return Current.TryGetValue(from, out to); }
    ////}
}
