using System;
using System.Collections.Generic;
using System.Diagnostics;

namespace Merrigan0.NmlInternal {
    ////[Untested]
    ////private class BinaryOperator {
    ////    // A function that takes a left and right expression and returns the combining one
    ////    //public Func<Expression<object>, Expression<object>, BinaryExpression<object>> CreateExpression { get; private set; }

    ////    public Type ExpressionType { get; private set; }
    ////    public String Text { get; private set; }
    ////    public int Tightness { get; private set; }

    ////    public BinaryOperator(String text, Type expressionType) {
    ////        ExpressionType = expressionType;
    ////        Text = text;
    ////    }

    ////    //public BinaryOperator(String text, Func<Expression<object>, Expression<object>, BinaryExpression<object>> createExpression) {
    ////    //    CreateExpression = createExpression;
    ////    //    Text = text;
    ////    //}
    ////}

    ////private class BinaryNumberOperator : BinaryOperator {
    ////    public Type IntegerExpressionType { get; private set; }
    ////    public Type RealExpressionType { get; private set; }

    ////    public BinaryNumberOperator(String text, Type integerExpressionType, Type realExpressionType) : base(text, null) {
    ////        IntegerExpressionType = integerExpressionType;
    ////        RealExpressionType = realExpressionType;
    ////    }

    ////}
}
